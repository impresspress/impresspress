# Field Notes Press

One-time digital product with a public `license` select and lookup pricing. Seed `commerce.fixture.json`, publish the immutable offer, then embed product `field-guide` with hosted Checkout. Grant the download entitlement only after the authoritative settled-payment event; the browser return is informational.

The deterministic journey selects the Studio license, previews USD 79.00, creates hosted Checkout, and verifies the exact input snapshot sent by the real static widget.
